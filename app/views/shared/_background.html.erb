<div class="background <%= action_name %>"></div>
<% page = action_name %>
<%= video_tag(
  "#{page}-bg.mp4",
  class: 'background',
  autoplay: true,
  loop: true,
  muted: true 
  ) if action_name == "home" || action_name == "contact"          
  %>