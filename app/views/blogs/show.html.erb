<div class="col-sm-8 blog-main">
  <h2> <%= @blog.title %></h5>
  <h5> Topic: <%= @blog.topic.title %> </h5>
  <%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin)%> 
  <hr>
  <p>
    <%= @blog.body %>
  </p>

  <%= render 'comments/comment_form' %>


  <div id="comments" data-blog-id="<%= @blog.id %>" >
  <%= render @blog.comments %>
  </div>

</div>







